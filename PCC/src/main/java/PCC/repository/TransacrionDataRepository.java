package PCC.repository;

import PCC.model.TransactionData;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TransacrionDataRepository extends JpaRepository<TransactionData, Long> {
}
